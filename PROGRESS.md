# 量词大冒险 — 进度记录

一年级华文「5.3 认识各类词语——量词」的自主学习 + 闯关游戏。改编自 `../../mario-game/index.html`
（Phaser 3 像素闯关引擎），题库换成 17 个量词，前面加了强制学习 slides + 分数达标闸门。

## 怎么玩（本地试玩）

```
cd teaching-tools/tahun1-bc-liangci
python3 -m http.server 8744
```
浏览器打开 http://localhost:8744/ 。

## 整体流程
1. **选人**：选班级（1I / 1G，沿用《一年级华文闯关问答》同一份名单）→ 点自己的名字
2. **Slides 学习**（19 页：1 页量词概念介绍 + 17 个量词 + 1 页总结）：
   - 门槛不分「第一次学习」和「答错重学」，都一样：把 17 个量词、102 个例子短语（如「一座大山」）**逐一点击确认读过**，全部点完「开始闯关游戏」才解锁——没有时间限制，因为点完 102 个已经需要一定时间，不需要额外计时
   - 每页至少停留 0.8 秒才能翻下一页（防止狂点跳过）
3. **游戏闯关**：Phaser 版 Mario，一轮固定 **10 题**（题库 119 题随机抽 10，不重复），沿用原本双段跳/坑洞/终点旗杆机制，另外加了：
   - 地面怪物（青蛙，巡逻+可踩）+ **天上的怪兽**（老鹰，飞行巡逻+轻微上下浮动，可踩可撞）
   - 每个问答站除了原本的问答方块+砖块，额外在旁边、比较低的位置加了一个**垫脚的矮箱子**（左右随机），当台阶用——先跳上矮箱子，再跳上大箱子，不是叠在大箱子正上方
   - **每一轮关卡布局都会重新生成**：问答方块高度（3.6/4.4/5.0 个 TILE 三种随机）、垫脚箱子左右位置、地面怪物/天上老鹰的位置・巡逻范围・初始方向・飞行高度，都是每段关卡各自独立随机，不会每 620px 就长得一模一样
4. **结算**：正确率 ≥ 70%（`PASS_RATE` 常量可调）算达标
   - 达标：可以「再玩一次」（新一轮 10 题）、「重新看资料」、看排行榜，或换人
   - 不达标：只能「重新学习资料」，强制回到 slides 重新点完全部 102 个例子，没有跳过游戏的路径
5. **排行榜**：历史最高分 + 累计挑战次数，阶段一先用 localStorage 模拟，接了 Google Sheet 后端后自动变成全班共享

## 文件结构
- `index.html` — 全部前端代码（选人/学习/游戏/结算/排行榜五个画面 + Phaser 场景）
- `Code.gs` — 排行榜后端（Google Apps Script，纯 JSON API，不含任何页面），部署方式见 `部署步骤.md`
- `assets/` — 直接复用 `mario-game/assets` 的 SunnyLand 美术素材（同一份文件，非任天堂角色，无版权顾虑）

## 关键设计决定
- **架构**：网页本体部署 Vercel/GitHub（静态站，跟 teaching-tools 其他工具一致）；共享排行榜用 Google Sheets 存资料，但只透过一个「只回传 JSON、学生看不到」的 Apps Script Web App 读写——用户明确要求不要整个网站放在 script.google.com 上面，只接受 Apps Script 当安静的 API 桥梁
- **学习门槛 = 点击确认，不计时**：一开始设计过「翻完 19 页 + 满 5 分钟」的计时门槛，用户反馈「点完 102 个卡片本来就要花不少时间了，不需要再计时」，改成不分场景统一用「102 个例子逐一点击确认」当门槛，答错重学时也是重新点一遍全部 102 个（`confirmedPhrases` 每次进入 slides 都会清空重来）
- **每轮 10 题**：`ROUND_SIZE` 常量控制，从最初的 20 题（用户嫌太长）调整为 10 题；关卡长度、怪物/宝石站点都跟着 `ROUND_SIZE` 自动生成
- **题库来源**：17 个量词、每个 6 个四字短语 + 1 个例句 = 119 题候选池，每轮 `shuffleArr().slice(0,ROUND_SIZE)` 随机抽（已用 node 脚本模拟验证：选项 4 个不重复、答案索引有效）
- **达标标准**：正确率门槛（用户选择），不是错题数上限
- **踩坑记录：宝石卡进箱子里**——最初每段关卡中间那颗装饰宝石的 x 坐标跟砖块几乎重叠（只差 2px），学生反馈"看起来像卡在箱子里"；修正为明确放在砖块正上方、留出清楚的垂直间距，改用 `{x,y}` 物件取代原本靠 `i%3` 猜高低的写法，逻辑更直观也更不容易再犯同样的错
- **踩坑记录：矮箱子位置理解错了**——第一版把矮箱子叠在大箱子正上方（纯装饰），用户反馈这样没有用，矮箱子应该放在大箱子「旁边的下面」当垫脚台阶，才有实际的攀爬用途；改成 `STEP_SPOTS`，位置在 `qx`/`bx` 左右两侧、高度比大箱子矮 2～2.6 个 TILE，形成「地面→矮箱子→大箱子」的三段式攀爬节奏
- **关卡布局改成每轮重新生成**（`generateLevelLayout()`），不再是模块加载时算一次的固定 `const` 数组——原本每段关卡的方块高度/怪物位置/宝石位置都是完全相同的相对偏移，用户反馈「玩起来太单调，好像一直在重复」；改成 `let` + 可重复调用的生成函数，每次 `startNewRound()` 都重新洗一次牌（已用 node 脚本模拟 200 轮验证：垫脚箱不会掉进坑里、位置不会跑出关卡范围）
- **踩坑记录：地面怪物随机巡逻把玩家卡在坑洞前**——加随机化时地面青蛙的巡逻右边界 `x+range` 算出来可能超过坑洞起点（620 关卡宽度里坑在 `+400` 起），玩家反馈"卡住了过不去"，实际原因是青蛙巡逻到坑边把玩家一直撞退，没办法助跑跳过去；修正为巡逻范围改存明确的 `minX`/`maxX`，并强制 `maxX` 不超过 `segX+370`（离坑洞起点留 30px 安全缓冲），已用 node 脚本模拟 5000 次确认 `maxX` 恒小于坑洞起点

## 已上线
- GitHub：`kongsi-idea/tahun1-bc-liangci`，Vercel：https://tahun1-bc-liangci.vercel.app
- 已登记进点子铺首页 `kongsi-idea/app.js`（DSKP 5.3.1 对照已核实，见 `docs/dskp/tahun1/bahasa-cina.md`），已部署上线
- 共享排行榜后端还没部署，`API_URL` 目前是空字符串，排行榜数据只存在打开网页那台设备的浏览器里——用户选择先上线、之后再接 Google Sheet 后端

## 已知/待处理
- Vercel 项目的 GitHub 自动部署连接失败过一次（`vercel git connect` 报错，可能是 GitHub App 权限还没授权到这个新仓库），目前是手动 `vercel deploy --prod` 上线的，之后改代码要记得手动重新部署，或请用户去 Vercel 后台补权限
- 点子铺首页那张工具卡缺缩略图（`thumbnails: []`），这次开发环境 Playwright 被别的会话卡住的浏览器进程占用，没能截到真实画面，之后要找机会补 3-4 张
- 没有做移动端触屏控制（跟原版 mario-game 一样，只支持键盘）
- 每轮关卡布局改成随机生成后，还没有请用户实机确认新的垫脚箱子攀爬节奏、老鹰不同高度的手感是否都合适

## 下一步
等用户实机试玩确认这几轮调整（垫脚箱子位置、关卡随机变化、10 题长度、天上老鹰）没问题，再讨论要不要接 Google Sheet 共享排行榜。
